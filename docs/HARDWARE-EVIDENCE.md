# Sanitized source-milestone evidence

Recorded source fixture run on ESP32-P4 Rev1.3, Waveshare 4.3-inch display/touch board:

```text
I esp_psram: Found 32MB PSRAM device
I esp_psram: Speed: 200MHz
fixture: compute done rc=0 pass=1 internal_free=309363 stack_watermark=27584
```

These are selected non-identifying fields from the source fixture boot transcript;
timestamp prefixes were removed. Display/touch operation was reported by the hardware
operator. The source ran real Rust BIP39/BIP32/BIP84 computation, not prerecorded
result text. The excerpt is not a substitute for independent reproduction.

No device IDs, hardware addresses, serial paths, host identifiers, raw transcripts,
photos, flash images or recovery backups are published. This report covers the
previous source fixture, not a fresh target build/flash of the standalone layout.
The host-only result and build-validation limitations are recorded separately.
